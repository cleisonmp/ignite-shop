import { styled } from '../../../styles'

export const Container = styled('footer', {
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  width: '100%',
  maxWidth: 1180,
  marginBottom: 0,
  marginLeft: 'auto',
  marginRight: 'auto',
})
